# To-Do List Application — Документация проекта

##  Обзор проекта

Простое консольное приложение для управления списком задач (To-Do List) с возможностью сохранения данных в файл. Приложение демонстрирует базовые принципы работы с файлами, обработку пользовательского ввода и организацию кода на Python.

##  Функциональность

- **Добавление задач** — пользователь может вводить новые задачи
- **Просмотр задач** — отображение всех задач с нумерацией
- **Удаление задач** — удаление задачи по её порядковому номеру
- **Автосохранение** — все изменения немедленно сохраняются в файл
- **Персистентность** — задачи сохраняются между запусками программы

## 🛠 Технологии

- Python 3.x
- Модуль `os` для работы с файловой системой
- Работа с текстовыми файлами (чтение/запись)
- Консольный интерфейс пользователя

## 📁 Структура проекта

```
todo-app/
│
├── to-do-list.py              # Основной файл программы
├── tasks.txt            # Файл для хранения задач (создаётся автоматически)
└── README.md           # Документация
```

## 💻 Детальный разбор кода

### 1. Импорт и константы

```python
import os

FILENAME = "tasks.txt"
```

- `import os` — импорт модуля для проверки существования файла
- `FILENAME` — константа с именем файла для хранения задач

### 2. Функция загрузки задач

```python
def load_tasks():
    """Загружает задачи из файла."""
    if not os.path.exists(FILENAME):
        return []
    with open(FILENAME, "r", encoding="utf-8") as file:
        return [line.strip() for line in file.readlines()]
```

**Как работает:**
1. Проверяет существование файла с задачами
2. Если файла нет — возвращает пустой список
3. Если файл есть — читает все строки, удаляет символы переноса (`strip()`)
4. Возвращает список задач

**Особенности:**
- Кодировка UTF-8 для поддержки русского языка
- List comprehension для компактного кода
- Автоматическое закрытие файла (менеджер контекста `with`)

### 3. Функция сохранения задач

```python
def save_tasks(tasks):
    """Сохраняет задачи в файл."""
    with open(FILENAME, "w", encoding="utf-8") as file:
        for task in tasks:
            file.write(f"{task}\n")
```

**Как работает:**
1. Открывает файл в режиме записи (`"w"`)
2. Построчно записывает каждую задачу
3. Добавляет символ переноса строки после каждой задачи

**Важно:** Режим `"w"` перезаписывает файл полностью, что гарантирует актуальность данных

### 4. Функция отображения задач

```python
def show_tasks(tasks):
    """Выводит список задач."""
    if not tasks:
        print("\n[!] Список задач пуст.")
    else:
        print("\n--- Ваши задачи ---")
        for idx, task in enumerate(tasks, 1):
            print(f"{idx}. {task}")
```

**Ключевые моменты:**
- Проверка на пустой список
- `enumerate(tasks, 1)` — автоматическая нумерация с 1 (не с 0)
- Читаемый формат вывода с разделителями

### 5. Функция добавления задачи

```python
def add_task(tasks):
    """Добавляет новую задачу."""
    task = input("Введите описание задачи: ").strip()
    if task:
        tasks.append(task)
        save_tasks(tasks)
        print(f" Задача '{task}' добавлена!")
```

**Особенности реализации:**
- `strip()` удаляет лишние пробелы в начале и конце
- Проверка на пустую строку
- Немедленное сохранение после добавления
- Визуальная обратная связь для пользователя

### 6. Функция удаления задачи

```python
def remove_task(tasks):
    """Удаляет задачу по номеру."""
    show_tasks(tasks)
    if not tasks:
        return
    try:
        num = int(input("Введите номер задачи для удаления: "))
        if 1 <= num <= len(tasks):
            removed = tasks.pop(num - 1)
            save_tasks(tasks)
            print(f" Задача '{removed}' удалена.")
        else:
            print(" Неверный номер.")
    except ValueError:
        print(" Пожалуйста, введите число.")
```

**Комплексная обработка ошибок:**
1. `try-except` для перехвата ошибок преобразования типов
2. Проверка диапазона индексов (1 до длины списка)
3. Обработка пустого списка
4. `.pop()` — удаляет элемент и возвращает его значение

### 7. Главный цикл программы

```python
def main():
    """Основной цикл программы."""
    tasks = load_tasks()
    print("📋 Welcome to the Professional To-Do App!")
    
    while True:
        print("\n1. Добавить | 2. Показать | 3. Удалить | 4. Выход")
        choice = input("Выберите действие: ")

        if choice == '1':
            add_task(tasks)
        elif choice == '2':
            show_tasks(tasks)
        elif choice == '3':
            remove_task(tasks)
        elif choice == '4':
            print(" До свидания!")
            break
        else:
            print(" Неизвестная команда.")
```

**Архитектура:**
- Бесконечный цикл `while True`
- Меню с 4 опциями
- Обработка невалидного ввода
- Условие выхода (`break`)

### 8. Точка входа

```python
if __name__ == "__main__":
    main()
```

Идиома Python для гарантии, что код выполняется только при прямом запуске скрипта, а не при импорте.

##  Использование

1. Сохраните код в файл `todo.py`
2. Запустите программу:
```bash
python todo.py
```

3. Следуйте инструкциям в меню:
   - **1** — добавить новую задачу
   - **2** — показать все задачи
   - **3** — удалить задачу по номеру
   - **4** — выйти из программы

## 📊 Пример работы

```
 Welcome to the Professional To-Do App!

1. Добавить | 2. Показать | 3. Удалить | 4. Выход
Выберите действие: 1
Введите описание задачи: Купить продукты
 Задача 'Купить продукты' добавлена!

1. Добавить | 2. Показать | 3. Удалить | 4. Выход
Выберите действие: 2

--- Ваши задачи ---
1. Купить продукты
```

##  Изученные концепции Python

Этот проект демонстрирует:

1. **Работа с файлами** — чтение и запись текстовых файлов
2. **Обработка ошибок** — try/except блоки
3. **Списки** — добавление, удаление, индексация
4. **Функции** — декомпозиция кода
5. **Менеджеры контекста** — `with open()`
6. **Модуль os** — работа с файловой системой
7. **List comprehension** — компактное создание списков
8. **enumerate()** — нумерация элементов
9. **Ввод/вывод** — взаимодействие с пользователем

##  Возможные улучшения

- [ ] Добавить отметку о выполнении задачи
- [ ] Редактирование существующих задач
- [ ] Сроки выполнения (дедлайны)
- [ ] Приоритеты задач
- [ ] Категории/теги
- [ ] Дата создания задачи
- [ ] Поиск по задачам
- [ ] Графический интерфейс (Tkinter)
- [ ] База данных вместо текстового файла

##  Примечания

- Все задачи хранятся в формате plain text
- Программа полностью кроссплатформенная
- Код соответствует PEP 8 (стиль кодирования Python)
- Добавлена поддержка русского языка в комментариях и выводе
